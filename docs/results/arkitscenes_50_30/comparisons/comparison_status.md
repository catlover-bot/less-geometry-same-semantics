| baseline | condition | group | execution | availability | status | alignment | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LGS input denoising adaptation | extreme_compression | main | local | available_now | completed_local | json=native, relations=native, scene=native | Lightweight deterministic test-time input adaptation baseline inspired by CloudFixer-style robustness comparisons. |
| LGS input denoising adaptation | severe_corruption | main | local | available_now | completed_local | json=native, relations=native, scene=native | Lightweight deterministic test-time input adaptation baseline inspired by CloudFixer-style robustness comparisons. |
| LGS lightweight structured | clean | main | local | available_now | completed_local | json=native, relations=native, scene=native | Native lightweight structured baseline from this repository. |
| LGS lightweight structured | severe_corruption | main | local | available_now | completed_local | json=native, relations=native, scene=native | Native lightweight structured baseline from this repository. |
| SpatialLM imported structured outputs | clean | main | external_import | imported | imported | json=converted, relations=mapped, scene=mapped | Heavy upper-bound comparison path. Full in-repo integration is intentionally out of scope; evaluate exported scene-level structured outputs instead. |
| SpatialLM imported structured outputs | severe_corruption | main | external_import | imported | imported | json=converted, relations=mapped, scene=mapped | Heavy upper-bound comparison path. Full in-repo integration is intentionally out of scope; evaluate exported scene-level structured outputs instead. |
| VoteNet imported detections | clean | main | external_import | imported | imported | json=converted, relations=derived, scene=derived | Standard non-LLM 3D detection baseline evaluated after converting 3D detections into the shared coarse semantic JSON space. |
| VoteNet imported detections | severe_corruption | main | external_import | imported | imported | json=converted, relations=derived, scene=derived | Standard non-LLM 3D detection baseline evaluated after converting 3D detections into the shared coarse semantic JSON space. |
| 3DGraphLLM imported outputs | clean | supplementary | external_import | pending_external | pending_external | json=converted, relations=mapped, scene=mapped | Supplementary only because the native task framing is broader than this benchmark's coarse semantic JSON. Canonical imported manifest not found at outputs/external_baselines/3dgraphllm/clean_predictions.json. Run the appropriate ingestion script after copying server outputs back locally. |
| MiniGPT-3D imported outputs | clean | supplementary | external_import | pending_external | pending_external | json=converted, relations=mapped, scene=mapped | Supplementary only because the native task alignment is captioning/QA-oriented rather than coarse structured scene semantics. Canonical imported manifest not found at outputs/external_baselines/minigpt3d/clean_predictions.json. Run the appropriate ingestion script after copying server outputs back locally. |
| PointLLM imported outputs | clean | supplementary | external_import | pending_external | pending_external | json=converted, relations=mapped, scene=mapped | Supplementary only because the native tasks focus on open-ended point-cloud language understanding rather than this benchmark's coarse semantic JSON. Canonical imported manifest not found at outputs/external_baselines/pointllm/clean_predictions.json. Run the appropriate ingestion script after copying server outputs back locally. |
