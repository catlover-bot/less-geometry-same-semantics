# Main Claims Template

Use `claims/frozen_claims.md` as the source of claims that are currently supported by completed runs.

Candidate claims to verify:

1. Aggressive degradation preserves a substantial fraction of coarse semantic performance.
2. Graph bottlenecks improve robustness under corruption.
3. Schema-constrained structured decoding improves validity or semantic stability.
4. Compression preserves coarse object semantics more than fine relation structure.

Do not include claims from this template unless the frozen-claims report marks them as supported.
